// Placeholder module. See ../components/Inbox.tsx for why this empty module exists
// (supersedes a removed scaffold "first run" file under a delete-less sync).
// The Contextlens demo's data lives in ./scenario.ts.
export {}
