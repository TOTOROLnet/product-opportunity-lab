{
  "name": "mneme-jiheng-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mneme (记衡) — a recall-time governance & hygiene control plane for user-owned cross-tool AI memory: before any memory is injected into a tool's context, scope it (least-privilege) and vet it (provenance / contradiction / staleness / poisoning). Static frontend demo (mock data).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
