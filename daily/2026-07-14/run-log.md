# Run Log — 2026-07-14

## 使用的报告
- `daily/2026-07-14/source-report.md`（由 `scripts/collect_recent_reports.py --days 1` 自公共仓库 product-hunt-radar 拉取，最新一份 = 2026-07-14.md）。
- 报告基调：技术向明显变厚，Agent 基础设施在「运行时 / 数据接入 / 支付计费 / 安全红队」四层同时冒新品（Osaurus / AgentKey / Loomal / Fabraix）；2C 当日空窗。

## Loop 1 — 机会发现（客观 + 创新）
- 客观提取信号：AgentKey（15/18，统一接入 + **调用前成本可见**）、Loomal（14/18，**x402 按次付费**）、Osaurus（16/18，本地 harness）、Fabraix（14/18，对抗式安全）。
- 独立判断：当日最强、最可辩护的结构性信号是趋势 2「**让 agent 自己付费/按次计费**」（Loomal + AgentKey ≥2 产品同向 + 结构性变化），也是报告"最想跟进方向"（预算约束地花钱）。我判断报告只看到"能付钱（rail）"和"知道价（成本可见）"两层，**漏掉第三层**：付钱前"该不该付、值不值"，付钱后"这钱买到了什么"。
- 候选（五维/25）：
  - A 值当 Worthwhile（花费策略 + 对账层）：4+5+5+5+4 = **23**
  - B 预算护栏·急停（执行型花费上限/急停）：4+3+4+4+3 = 18（与既往 Fusebox/Mandate 执行治理重叠、且部分被平台内建覆盖）
  - C 品味库 Taste Vault（2C，审美沉淀）：3+3+4+4+3 = 17（当日 2C 空窗、信号支撑弱，诚实给低分）
- 选择 **A 值当（23/25）**：正中最强信号、切报告未点出的一层、与执行型治理区隔、可 3 分钟讲清。
- 门槛：23 ≥ 16 → 进入 Demo。

## Loop 2 — Demo 设计
- 机会属抽象/基础设施类 → 采用"模拟体验 + 价值可视化"：以 before/after（贪婪 vs 值当）为主轴 + 可交互规划器（预算/够用标准滑块）+ 逐笔价值归因收据。
- 诚实分型声明：价格/覆盖/质量为手工 mock；规划算法（择优/去重/够用即止/预算封顶 + 价值归因）为前端真实运行。
- 三视图（单页切换）：任务与预算 / 对比运行 / 对账收据；含 CONTROL 场景反自嗨。

## Loop 3 — Demo 开发
- 技术栈 Vite + React + TypeScript；`vite.config.ts` 设 `base: './'`。
- 结构：`types.ts` 领域模型；`data/scenarios.ts` 3 个 mock 场景（含 CONTROL）；`logic/planner.ts` 真实规划 + 价值归因算法；`components/` 三视图 + 裁决徽章；`App.tsx` 状态与视图切换。
- 关键算法：有效覆盖 = 覆盖×可靠度（低于可用下限 0.3 记 0）；需求覆盖 = 已调源最高 effQ（天然去重）；边际价值按够用线截断；反复挑边际价值/价格最高者、够用即止、预算封顶；裁决 值/冗余/浪费。

## Loop 4 — 自动验证（硬检查）
- `bash scripts/validate_demo.sh daily/2026-07-14/demo` → **通过**（npm install ok；`tsc -b && vite build` 一次成功；dist/index.html 非空含 `id="root"`；1 个 JS bundle）。
- **build 修复轮次：0/3。**
- headless 复算（vite-node 跑 planner）确认三场景数值：
  - 竞品调研：贪婪 $3.45 → 值当 $0.51，省 $2.94（85%），5 值/3 冗余/1 浪费，进度 100%/100%。
  - 新品测评：贪婪 $3.17 → 值当 $0.92，省 $2.25（71%），5 值/2 冗余/1 浪费，100%/100%。
  - CONTROL 本地化：$0.21 → $0.21，省 $0.00（0%），nearOptimal=true，4 值，100%/100%。
- 浏览器实测（preview:4173，computer-use）：三视图正常渲染、无白屏/报错；场景 1 收据「省下 $2.94（85%）· 质量对等」，CONTROL「已接近最优 · 仅可省 $0.00（0%）」，与复算一致；截图存 `screenshots/`。

## Loop 5 — 体验自评
- 见 `evaluation.md`，结论 **PASS**。

## 遇到的问题
- 无阻塞问题。设计规划算法时注意到"gain/price 会偏好极便宜低质源"，通过引入"可用下限 USEFUL_FLOOR=0.3（低于此覆盖记 0）"解决，使低质"博客抓取/低价聚合"正确判为浪费。

## 最终结论
- 达门槛（23/25）+ build 成功（0 修复轮）+ 产物齐全 + 浏览器实测通过 → **PASS / ok**。
