# Run Log — 2026-08-24

## 使用的报告
- `source-report.md`（product-hunt-radar，2026-08-24）。由 `python3 scripts/collect_recent_reports.py --days 1` 自公共仓库浅克隆拉取，latest = `2026-08-24.md`。
- 报告核心：当日「近乎全量延续」，唯一新 AI-core 品为 **Construct Computer**（每个 agent 一台按需计费云电脑，Cloudflare 边缘）；提炼出「每个 agent 一台电脑」的成本工程命题与「隔离/可靠/单位成本」不可能三角；**当日无高价值 2C 新品**。

## Loop 1 — 机会发现
- 客观区分「报告事实」与「我的独立判断」：报告把「成本被拖垮」当 Construct 卖点被动陈述；我重估为**上线前、独立的架构×定价决策痛点**。
- 生成 4 个候选并五维打分：
  - A 召算（运行时成本×毛利决策沙盘）= **23/25**
  - B 散场（闲置休眠策略设计器）= 19/25
  - C 电表（事后成本归因 FinOps）= 18/25
  - D 可玩（2C prompt→可玩 feed，对照 Pocket）= 13/25（诚实低分：近克隆、AI-core 纯前端难自证）
- 选中 A（23/25，≥16 门槛）→ 进入 Demo。2C 侧已评估但当日信号弱，择技术向为基于分数的客观结论。

## Loop 2 — Demo 设计
- 归类：抽象/基础设施类 → 「模拟体验 + 价值可视化」。
- 3 页：沙盘（滑块+四架构卡片+毛利×压力维度曲线+不可能三角+顾问解读）/ 架构对比（本地·云端常驻·边缘按需 before/after）/ 方法论与透明度。

## Loop 3 — Demo 开发
- Vite + React + TS；`vite.config.ts` 设 `base:'./'`；手写 SVG 图表（无第三方图表库，降低构建风险）。
- 确定性成本模型 `src/lib/costModel.ts`；脚本化顾问 `src/lib/advisor.ts`（非真实 LLM）。
- **关键设计修正**：首版成本常量导致默认画像四架构全负（token 成本 $0.6/busy-h 压过营收），且发现毛利对用户数**线性→尺度不变**，原「毛利转负 @ 用户数」曲线会是平线。据此：
  1. 重新标定常量（token 降为 $0.15/busy-h，让计算/机器成本成为架构间主要差异项；VPS $40/2 会话、云 $0.12/开机小时、边缘 $0.18/忙碌小时+会话开销、本地 $0.25/用户控制面）。
  2. 甜点改为**风险调整推荐分**（毛利率 − 运维/风险惩罚：VPS 20 / 云 6 / 边缘 12；本地因不满足自治不入选）。
  3. 曲线改为**压力维度扫描**（闲置比 / 鲸鱼放大 / 用户规模三选一），并新增「单位经济由用量形态×定价决定、非用户数」的反直觉洞察。
- 校准后甜点随场景变化：默认→边缘按需(68%)、法务→边缘、客服→云端常驻、research→自建 VPS，三种自治架构都能在不同场景成为甜点（非退化）。

## Loop 4 — 自动验证
- `bash scripts/validate_demo.sh daily/2026-08-24/demo`：**1 轮通过**（npm install 成功、build 成功、dist/index.html 非空含 root、1 个 JS bundle）。0 次修复。
- 用 `node` 脚本独立复核成本模型算术，与页面显示一致。

## Loop 5 — 体验自评
- `npm run preview` 起服务 + computerUse 浏览器实测：三页均渲染、无空白/报错；拖动闲置比 60%→80% 使云端常驻毛利 58%→30%；压力轴切换生效；场景切换甜点漂移正确；对比页三列与方法论表正常。截图存 `screenshots/`。
- 结论：**PASS**。

## 遇到的问题
- 成本常量首版失真（见 Loop 3 修正），已通过重标定 + 独立算术复核解决。
- 图表原设计为「随用户规模」的悬崖，与线性模型不符，已改为压力维度扫描并诚实揭示尺度不变性。

## 最终结论
- status = **PASS**，reason = **ok**。达门槛机会（23/25）+ build 成功 + 必需产物齐全。
